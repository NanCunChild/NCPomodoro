// 有点迷糊了，这个文件是干嘛的？
// 有时间再研究一下。

var filters = {
  toFix: function (value) {
    return value.toFixed(2) // 此处2为保留两位小数 
  }
}

module.exports = {
  toFix: filters.toFix,
  toNumber: filters.toNumber,
}
